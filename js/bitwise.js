/**
 * @author kubap
 */

Bit = function(number){
  var _nr = Number(number);
  
  this.or = function()
  {
    
  }
  
  this.and = function()
  {
    
  }
  
};

Bit.toNumber = function(a) 
{
  return Number(a);
}

Bit.prototype.or = function(number){}
